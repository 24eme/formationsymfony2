{% extends  'base.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block body %}

{{ form_start(form, {
    attr: {novalidate : 'novalidate'}
  })
}}

{#{ form_start(form) }#}

<table >
<tr>
  <!-- td>{{ form_row(form.id) }}</td -->
  <td>{{ form_row(form.date) }}</td>
  <td>{{ form_row(form.scoreA) }}</td>
  <td>{{ form_row(form.scoreB) }}</td>
  <td>{{ form_row(form.rencontre) }}</td>
  <td>{{ form_row(form.nbCafe) }}</td>
</tr>
</table>
</br>
<!-- button type= "submit" class="btn btn-default">Modifier</button -->

{{ form_end(form)}}

{% endblock %}
