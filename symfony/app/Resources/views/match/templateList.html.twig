<table class="table table-hover">
    <thead>
        <tr>
            <th>Date</th>
            <th>Libellé</th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {% for entry in entries %}
            <tr>
                <td>{{entry.date | date('d/m/Y')}}</td>
                <td>{{entry.libelle}}</td>
                <td>
                    {% if entry.equipeA %}
                        <a href="{{ path('equipe_route', {'team':entry.equipeA}) }}">{{entry.equipeA}}</a>
                    {% endif %}
                </td>
                <td>{{entry.scoreA}}</td>
                <td>-</td>
                <td>{{entry.scoreB}}</td>
                <td>
                    {% if entry.equipeB %}
                        <a href="{{ path('equipe_route', {'team':entry.equipeB}) }}">{{entry.equipeB}}</a>
                    {% endif %}
                </td>
                <td>
                    <a href="{{ path('modifier_route', {'id':entry.id}) }}">Modifier</a>
                </td>
                <td>
                    <a href="{{ path('pronostic_route', {'idRencontre':entry.id}) }}">Pronostiquer</a>
                </td>
                <td>
                    <a href="{{ path('list_route', {'idRencontre':entry.id}) }}">Pronostics</a>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>
